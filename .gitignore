# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env

# Jupyter
.ipynb_checkpoints/

# OS / IDE
.DS_Store
.idea/
.vscode/

# Temporary Microsoft Office lock files
~$*.docx
~$*.xlsx
~$*.pptx

# Raw SEC submissions downloaded from SEC EDGAR
data/raw/sec_submissions/
data/raw/companyfacts/

# Reproducible SEC filing caches (FilingSummary, rendered statements, reviews)
data/raw/sec_filings/

# Filing-first historical-universe SEC caches (master index, SUB-only extracts,
# and bounded same-accession submission headers). Rebuilt from the manifest.
data/raw/sec_historical_universe/

# Compact point-in-time inputs used only to apply the two frozen components.
data/raw/research_universe_target_application/

# Large generated SEC facts table
data/interim/sec_facts_long.csv

# Large generated row-level PIT-B artifacts
data/interim/target_candidate_v2_pit_b.csv
data/reports/target_candidate_v2_pit_b_revision_deltas.csv

# Train-only resolver-correction projections and rebuilt row-level artifacts.
# Exact hashes, row counts and provenance remain in the v1.1/v1.2 manifests.
data/interim/research_universe_target_application_v1_train_access_projection.csv
data/interim/target_candidate_v2_pit_b_v1_0_0_train_access_projection.csv
data/interim/target_candidate_v2_pit_b_v1_1_0_train.csv
data/interim/target_candidate_v2_pit_b_v1_2_0_train.csv
data/interim/x_t_pit_v1_0_0_train_access_projection.csv
data/interim/x_t_pit_v1_1_0_train_source_rebuild_candidate.csv

# Historical-universe row-level staging outputs. The canonical processed panel
# and compact audit/manifest remain visible for intentional versioning.
data/interim/research_universe_pit_candidates.csv
data/interim/research_universe_pit_unresolved.csv
data/interim/research_universe_pit_v1_1_0_target_pit_b_v1_0_0_working.csv

# Canonical historical-universe panel (~69 MB). Reproducible from SEC caches;
# its exact hash and row count are recorded in the versioned build manifest.
data/processed/research_universe_pit.csv
data/processed/research_universe_pit_v1_1_0_target_pit_b_v1_0_0.csv

# Large reproducible raw point-in-time feature artifact and interrupted build.
# The versioned build manifest records its exact schema, row count and hash.
data/processed/x_t_pit_v1_raw.csv
data/processed/x_t_pit_v1_raw.csv.tmp
data/processed/x_t_pit_v1_raw.csv.refresh.tmp
data/processed/research_universe_pit_v1_1_0_target_pit_b_v1_1_0_train.csv
data/processed/research_universe_pit_v1_1_0_target_pit_b_v1_2_0_train.csv
data/processed/x_t_pit_v1_1_0_train.csv

# Large row-level revision diagnostic; compact summaries remain in the X_t
# audit JSON/Markdown and are reproducible from the frozen inputs.
data/reports/x_t_pit_v1_revision_diagnostics.csv

# Download-run manifests; summary results remain in the audit reports
data/reports/target_candidate_v2_pit_b_revenue_evidence_downloads.json
data/reports/research_universe_target_application_revenue_evidence.json
data/reports/x_t_pit_v1_source_download.json
data/reports/target_candidate_v2_pit_b_revenue_review_downloads.json
data/reports/target_candidate_v2_pit_b_sec_shards.json

# Reproducible target-audit detail outputs. Canonical freeze reports and
# manually reviewed evidence remain versioned separately.
data/reports/target_candidate_v2_component_robustness_changed_observations.csv
data/reports/target_candidate_v2_missing_target_observations.csv
data/reports/target_candidate_v2_pit_b_audit.json
data/reports/target_candidate_v2_pit_b_audit.md
data/reports/target_candidate_v2_pit_b_manual_review.md
data/reports/target_candidate_v2_pit_b_manual_review_sample.csv
data/reports/target_candidate_v2_pit_b_revision_outliers.csv
data/reports/target_candidate_v2_pit_b_freeze_gate*
data/reports/target_candidate_v2_pit_b_final_revenue_resolver_*.csv

# LEGACY is intentionally not ignored. Archived source and documentation must
# be committed once so that moving them does not turn them into local-only files.
