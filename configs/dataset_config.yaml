# Active source-data scope used by Company Facts parsing and sanity checks.
# Modeling features, splits, and target rules are intentionally not defined
# here. The frozen target has its own versioned PIT-B configuration.
dataset:
  start_year: 2011
  # Source coverage includes 2025 only to retain the t+1 filing horizon for
  # feature year 2024; this does not authorize using test years for decisions.
  end_year: 2025
  forms:
    - 10-K
